# SEMICON 贊助商 Logo HTML 產生器使用說明

目前版本：**v5.1.1**

## 這個工具用來做什麼

這個工具用來整理 SEMICON Taiwan 贊助商 Logo，並產生可貼到 Drupal 的中英文 HTML。

它適合處理 Logo 分批提供、後續補連結、調整排序、更新級別等長期維護情境。

## 基本流程

1. 開啟 `index.html`。
2. 在「資料設定與管理」頁新增贊助商 Logo。
3. 選擇贊助級別。
4. 填入公司名稱與連結。
5. 檢查右側清單的狀態。
6. 調整排序或 Logo 顯示比例。
7. 切到「即時預覽與輸出」。
8. 選擇中文或英文預覽。
9. 複製中文或英文 HTML，貼入 Drupal。
10. 點選 `Save work file` 保存工作檔，方便之後繼續更新。

## 狀態說明

工具會自動判斷每個 Logo 的狀態。

| 狀態 | 意義 | 處理方式 |
|---|---|---|
| Ready | 已具備 Logo 與公司名稱，可輸出 | 可直接保留 |
| Check | 可輸出，但建議確認 | 依提示補名稱或連結 |
| Missing | 缺少必要 Logo 圖片 | 補上 Logo 後再輸出 |

上方摘要會顯示目前整體狀態，例如：

```text
4 logos · 2 ready · 0 missing · 2 check
```

如果仍有需要確認的項目，會顯示：

```text
Needs attention
```

如果全部都可發布，會顯示：

```text
Ready to publish
```

## Save work file / Load work file

因為贊助商 Logo 常常不是一次到齊，建議每次完成一輪整理後都保存工作檔。

- `Save work file`：下載目前工作檔。
- `Load work file`：載入之前保存的工作檔，繼續編輯。

工作檔包含：

- 贊助商資料
- 級別名稱設定
- 工具版本
- 匯出時間

## Sample

`Sample` 可以載入示範資料，用來快速了解輸出效果。

載入示範資料後，正式使用前請清空或替換成實際贊助商 Logo。

## 注意事項

- 請勿把尚未公開或未授權的 Logo 資產提交到公開 GitHub。
- 若要換電腦或交接給同事，請使用 `Save work file`。
- 如果只是關閉瀏覽器再回來，同一瀏覽器通常會保留本機暫存資料。
- 複製 HTML 前，請先確認預覽語言是中文或英文版本。
